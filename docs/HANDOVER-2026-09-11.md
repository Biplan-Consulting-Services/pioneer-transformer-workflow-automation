# Handover — morning of 2026-09-11, after the cutover

Written at 06:45 local, straight after the cutover finished. **Read this first, then
`CUTOVER-RUNBOOK.md`'s outcome table.** Everything below is either a live state you need
to know or a job waiting.

---

## The one thing that is not running

🔴 **The `Order Items` create-or-update trigger flow is DISABLED.** It was pasted (v002),
enabled, watched, and turned off again when it failed:

    WorkflowOperationParametersRuntimeMissingValue
    'Get_Model' ... 'id' may not be null or empty

`Get_Client` and `Get_Model` read the lookup id straight off the trigger row with no
guard, and **203 of 1,189 units have no Client, Model or Model Revision**. Editing one
fails the flow, and since `Condition` runs only on `Get_Model [Succeeded]`, the
`Status Date` auto-stamp is skipped with it.

**Consequences while it is off**, both confirmed acceptable with the user:

- no `Status Date` auto-stamp for anyone — staff type the date, as they always have
- `Order Items`' four `_TextField` mirrors go unmaintained — nothing uses them yet

**The fix is designed and written up** in `roadmap.md` under *FIRST daylight job*. Two
parts: guard both gets, and — the real one — decouple the stamp, which needs only
`Step Status` and `Step Status Stamped` and has no business depending on Client or Model.

⚠️ Before re-enabling: re-run the `StepStatusStamped = StepStatus` check (it was 262/262
at 05:55), and **test on one of the 203, not on a healthy unit.** Testing the happy path
is how this reached production.

---

## What is running

| | state |
|---|---|
| `Order Items` | 1,189 units, live, staff-facing |
| the four N3 sync flows | **all enabled** — Order, Model Revisions, Models, Clients |
| the transfer flow | **deleted** at 2.4. `.zip` kept against v010, the only re-importable artifact |
| the viewer | deployed to `Revue/FRM10-12.xlsx`, `Index` row 8 repointed, FRM09 confirmed returning rows |
| the old staff workbook | retired at `Revue/Formulaires/` — renamed and read-only |
| the trigger flow | 🔴 **off**, see above |

---

## Jobs waiting, in the order I would take them

1. **Fix and re-enable the trigger flow.** Design in `roadmap.md`. Everything else can
   wait; this one is a hole in the new system's behaviour.
2. **`x7_verify_parent_sync.js`** — read-only, expects `MISMATCH 0` across 46 mapped
   fields. Run it **after staff have made real edits**, so it tests the flows against
   genuine changes rather than the ones we made at 04:30.
3. **Measure the parent `_TextField` drift.** Three weeks unmaintained, and **the Power
   Apps filter, link and sort on those mirrors** — so this is a correctness problem in
   the apps, not housekeeping. A REST comparison per parent list says whether it is five
   rows or two hundred, which decides backfill versus flow. Roadmap has the design for
   maintaining them from the N3 flows, including the self-trigger hazard.
4. **`FRM10-12 Layout` as the default view.** Free now; the export trap it was parked for
   is behind us.
5. **The three French UI labels** in section 7 of the French handbook — on the Granby
   visit, along with the prior question of whether anyone there sees SharePoint in French
   at all.

---

## Things that will confuse you if nobody says them

- **REST and run-history timestamps in all these notes are UTC, four hours ahead of
  local.** The test log reads 08:35–08:47 for work done at 04:35–04:47.
- **The trigger flow's polling interval is 5 minutes, not the 1 that was pasted.** The
  designer rewrote it on save; the user did not set it and could not find the field. Any
  future paste of that flow reports FORKED on that one key with nobody having caused it.
- **A healthy transfer-flow run reported `Failed`.** That flow is gone now, but the habit
  it taught is still right: verify by reading data, never by run status.
- **`verify_*.py` scripts read the newest export.** After any schema change, re-export
  first or they answer about a list that no longer exists. This bit me once tonight: I
  "re-verified" after a column deletion against an export taken before it.
- **203 units have no Client, Model or Model Revision.** They are not broken; their
  Orders genuinely have no model. They are also the reason the trigger flow fails, and
  the reason `Mdl*`/`Rev*` columns are blank on some units.

---

## Verification tooling written tonight, all re-runnable

| script | answers |
|---|---|
| `rediff_units.py` | is every workbook unit on the list |
| `verify_port.py` | does every unit hold the same DATA — 18,946 values, 3 differ, all understood |
| `verify_status_split.py` | does `Step Status` + `Status Date` match Excel's composite |
| `verify_viewer.py` | runbook 3.2, the viewer's value conversions |
| `stamp_exports.py` | stamp and archive exports/workbooks, with the view-shaped-export trap check |
| `verify_n3_flows.py` | the four sync flows against the live column types |

Each one exists because eyeballing the equivalent had already produced a wrong answer at
least once.
