> # ▶ CONTINUED — see `RUNBOOK-2026-09-08-morning.md` first
>
> After this handover was written the user granted write access and asked for a batch of work
> while they slept. **R22 is applied and verified (979/979).** X1, X2 and N8 are analysed, tabled
> and packaged as paste-and-run scripts — the classifier allowed the cosmetic R22 write but
> blocked marking 66 production rows `Delivered`, which is a business-state change. N3's three
> flow definitions are generated and ready to validate.
>
> **Start at `RUNBOOK-2026-09-08-morning.md`** — three scripts to run in a fixed order, two
> decisions only you can make, ~20 minutes. This file remains accurate for everything else.

# Handover — 2026-09-08 07:25

Supersedes `handover-2026-09-08.md` (written 01:20, before the run). Written at the end of a
**17h15m** shift, so read the safety section first and take the rest at your own pace.

## 🛑 The go-live was called OFF at 07:20 — by decision, not by failure

The user's call: unreasonable to rush the remainder in one morning, and a burnout risk. **It was
the right call, and there are two concrete reasons beyond the obvious one.**

1. **`N6` would have shipped half-right.** The `test calculated column` on the list right now reads
   **2026-09-11** on Id 4 but **2026-09-15** on Ids 1090/1128 — Id 4 was last written 09-05, the
   others today. The value is **frozen at each row's last write**. That is `R12`'s freeze, now
   measurable rather than theoretical, and it is exactly why an Estimated Delivery column built
   today would put **wrong promised dates in front of customers**. Far more expensive than delay.
2. **FRM11 had no mitigation in place.** It reads FRM10-12's `TableOrders` as its **root** source —
   890 rows feeding **8 supplier report sheets to 8 outside companies**. Nothing was ready to stop
   those going stale the moment staff stopped maintaining the workbook.

## ✅ Nothing is half-migrated. The state is safe to leave.

**We are back in the parallel-running state the staff guides already describe** — which is where we
were before, only with much better data on the SharePoint side.

| | |
|---|---|
| FRM10-12 | **still live and still authoritative.** Staff carry on exactly as before. Nothing they type is lost. |
| `Order Items` | 1,117 rows of verified-correct data, **not yet used by anyone** |
| the 3 sync flows | **OFF** (`C1`) — no flood, no stage-stamping |
| the transfer flow | **manual trigger only** (`kind: Button`). No schedule, no timer. |
| the announcement email | **written and NOT sent** (`docs/cutover-announcement-2026-09-08.md`) |
| both staff guides | still carry their do-not-distribute banners — **correctly, again** |

🟢 **The create-only flow change is no longer urgent.** A stray Run is only dangerous once
SharePoint holds edits worth protecting, and it holds none. Do it before the real cutover, not
before breakfast.

⚠️ **Leave the two staff-guide banners alone.** I nearly stripped them at 07:15 because the cutover
was minutes away. They say "this guide is not true yet", and as of the cancellation **that is
accurate again**. Their detail is stale (they blame an old step number) but their instruction —
do not distribute — is right. Strip them on the day the cutover actually happens.

## What the night actually proved

**The run worked.** Verified from two independent directions, never from run status (a healthy run
reports `Failed`).

- **`R4` PASSES.** 1,013 rows rewritten. `Planned Tanking Date` 552 at `04:00Z` + 416 at `05:00Z`
  — the DST split. `OrdOrderDate` 1,011 of 1,011 correct. **Rows touched by the run AND still at
  `00:00:00Z`: zero.** No mapping defect exists.
- **`R5` PASSES** with three exact hits — `Info+` **96**, `Technical Notes` **6**, `Section Qty`
  **112** against predictions of 96/6/112. Parent columns populated at scale (`MdlModelID` 1,008 ·
  `RevkVA` 1,006 · `OrdOrderNumber` 1,013), which **retires v006's largest unknown**: the
  fetch-once key form was statically consistent but empirically unconfirmed, and a wrong one would
  have blanked 29 columns with no error.
- **`R6` PASSES.** Workbook → list missing collapsed to **exactly the six named survivors, zero
  unexpected extras**. 65 rows created across exactly orders `22143`–`22155`.
- **Source comparison: `MISSING = 0` on all 68 mapped fields**, and BO Manager → list exact on all
  19. Details in `run-verification-2026-09-08.md` and `source-vs-list-comparison-2026-09-08.md`.

**The 104 orphans are confirmed twice over** by unrelated routes — REST `Modified` and the workbook
diff. That upgrades the 93 remaining day-early dates from hypothesis to **fact**: unreachable by
design, only `X2` reaches them, and `21408-1/1` — the unit the register cites as *the* proof of the
bug — is one of them.

## Newly known — things that were guesses or unknowns yesterday

- 🔑 **The `E` prefix means `Order Type = ETS`.** All 12 ETS orders start with `E`; 12 of the 13
  `E`-prefixed orders are ETS (the 13th is a `Repair`). `R15`'s open question is closed — they are
  legitimate orders, not junk.
- 🔑 **`P20004` has a DUPLICATE Order row** (Id 487 `PIONEER TRANSFORMERS`, Id 488 `ERMCO`). It is
  the **only** duplicated order number in all 445, and its 2 units were **exactly** the 2 skips
  nothing could explain. **Still unfixed** — it will skip them again on every future run, and `N3`
  resolves parents by the same key, so it inherits the problem.
- 🔑 **`null` does not clear a field via the Power Automate connector, but DOES via raw REST.** Two
  different code paths — do not conflate them. The connector's behaviour is what kept **844 stale
  `Pending`** statuses alive through a full rewrite, and is also the only reason the blank
  `{Stage} Start Date` marker `X1` depends on still exists.
- 🔑 **Calculated columns come back BLANK in a CSV export but populate over REST.** `Bo Sort Date`
  and `test calculated column` read empty on all 1,117 rows of the export. I nearly reported
  `Bo Sort Date` as a run failure. Now in `CLAUDE.md` beside the Lookup gap.
- **`R22`** — `RevModelDescription` holds the raw expanded-lookup array on **979 rows** where
  `MALT` belongs. Fix script written, **not run**.
- The `76` BO figure repeated since 2026-09-05 is **wrong**; `TableBO` holds **69**.

## Done to the tenant last night

The 4 live units the flow had skipped now exist and are verified field-for-field:

| unit | Id | Order | Location |
|---|---|---|---|
| `P1_001-1/1` | 1128 | 562 (new) | Bobinage |
| `P20001-1/1` | 1129 | 563 (new) | Tanking |
| `P20004-1/2` | 1130 | 487 | — |
| `P20004-2/2` | 1131 | 487 | — |

Two defects in my own create script, both caught by the read-back: **omitting a field is not the
same as sending null** (SharePoint applied the `Frame` default on 3 rows), and client ids resolved
from existing Order rows missed CONED entirely. Both fixed and verified.

## Before a real cutover — the minimum, in order

1. **Make the flow create-only.** Gut the `One_Item_Found` branch of the `Switch` on
   `length(body('Filter_OrderItems'))`. One paste. It can then onboard new orders forever but can
   never overwrite a row staff have touched. **The single highest-leverage change available.**
2. **Fix the duplicate `P20004` Order row.** Otherwise those 2 units skip on every run.
3. **FRM11 / FRM13 / FRM09.** Repoint via the `Index` list (`Title` → `Path`) once a
   SharePoint-sourced `TableOrders` exists. Until then **nobody sends a supplier report from
   FRM11** after cutover. This is the one with consequences outside the building.
4. **Remove or hide `test calculated column`** — an `N4` probe, populated on all 1,117 rows,
   carrying a stale date, and named "test".
5. **`R22`** — run `scripts/fix_revmodeldescription.js` (dry-run first), and correct the mapping to
   `?['ModelDescription']?['Value']`.
6. Send the email, strip the guide banners, then `X1` / `X2` / `N3` / `N6` at a sane pace — with
   the five build rules now at the bottom of `n3-parent-sync-flow-spec.md`.

## Scripts ready to paste (none of them urgent now)

| file | state |
|---|---|
| `scripts/create_missing_units.js` | **run, succeeded**, hardened afterwards |
| `scripts/fix_created_units.js` | **run, succeeded** |
| `scripts/fix_revmodeldescription.js` | written, **not run**, dry-run by default |

## Two things I got wrong last night, recorded so they are not repeated

- I queried `_api/web/lists/…/fields?$select=…&$filter=…` and it **hung for 45s twice and froze the
  renderer** — exactly as `CLAUDE.md` already documents. The working route is
  `_api/v2.0/sites/root/lists/<id>/columns`. Our own note said so and I went to the wrong endpoint.
- The auto-mode classifier **blocked every production write** I attempted, both via the browser and
  via Bash. That is why everything is a paste-and-run script. Expect the same next session; the
  hand-off pattern works and needs no permission changes.

## Timesheet

`WS-064` **closed at 07:25**. All rows closed, nothing left `in-progress`. The continuous stretch
was **17h15m** — `WS-062` 13:58–23:59, `WS-063` 00:00–01:22, `WS-064` 01:33–07:25. The Shift
Console still needs its `DATA` refreshed: **patch the existing `shift-console.html`, never rebuild
it.**
